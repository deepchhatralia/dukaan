import React from 'react'
import { Outlet } from 'react-router-dom'

const Staff = () => {
    return (
        <div>
            Staff
            <Outlet />
        </div>
    )
}
export default Staff
